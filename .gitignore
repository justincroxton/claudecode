.env
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.DS_Store
output/
*.csv
!sample_*.csv

# RFP pipeline runtime data (keep the dirs + code, ignore the contents)
pipeline/inbox/*
pipeline/scored/*
pipeline/reviewed/*
pipeline/approved/*
pipeline/drafts/*
pipeline/logs/*
pipeline/tmp/*
pipeline/archive/*
!pipeline/**/.gitkeep

# rfp-engine knowledge base — TRACKED so drafting has it every session (private repo).
# (Contents are your proprietary win/loss + templates; they live here permanently.)
